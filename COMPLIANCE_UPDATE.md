# ✅ COMPLIANCE PERCENTAGE - ОБНОВЛЕНИЕ

## Что добавлено

Добавлен новый столбец **"Compliance"** в таблицу, который показывает **процент соответствия траектории разрешенному коридору**. Расчет выполняется **C++ программой** для максимальной производительности.

---

## 📊 Что показывает Compliance?

**Compliance Percentage** = процент точек траектории, которые:
- Находятся в пределах allowed_deviation от коридора ✅
- Не превышают allowed_speed ✅

### Цветовое кодирование:
- **Зеленый (>= 90%)** 🟢 - Отличное соответствие
- **Оранжевый (70-90%)** 🟠 - Приемлемое соответствие  
- **Красный (< 70%)** 🔴 - Требуется внимание

---

## 🔧 Как применить обновление

### Шаг 1: Перекомпилируйте C++ программу

```bash
cd /Users/ivan/PycharmProjects/FOFIS/cpp
make clean
make
```

**Что изменилось в C++:**
- Теперь выводит **3 значения** вместо 2:
  ```
  deviation speed_violation is_compliant
  ```
- `is_compliant` = 1 если точка соответствует, 0 если нет

### Шаг 2: Создайте миграцию базы данных

```bash
cd /Users/ivan/PycharmProjects/FOFIS
source venv/bin/activate
python manage.py makemigrations
python manage.py migrate
```

**Что добавлено в модель:**
- Новое поле `compliance_percentage` (FloatField)
- Рассчитывается C++ программой при обработке файлов

### Шаг 3: Перезапустите сервер

```bash
python manage.py runserver
```

### Шаг 4: Обновите страницу в браузере

```
http://127.0.0.1:8000/
```

Нажмите **Ctrl+F5** для принудительного обновления (очистка кэша).

---

## 📋 Проверка работы

### 1. Загрузите тестовые файлы

**Нормальный сценарий:**
```
sample_data/corridor.txt + sample_data/trajectory.txt
```
Ожидаемый Compliance: **~85-95%** (хорошее соответствие)

**Сценарий нарушения:**
```
sample_data/corridor_violation.txt + sample_data/trajectory_violation.txt
```
Ожидаемый Compliance: **~30-60%** (плохое соответствие)

### 2. Проверьте таблицу

В таблице должен появиться новый столбец **"Compliance"** между "Mean Speed" и "Actions".

```
┌──────┬─────────┬──────────┬─────────┬──────┬────────────┬─────┐
│Select│Corridor │Trajectory│   Dev   │Speed │ Compliance │ Del │
├──────┼─────────┼──────────┼─────────┼──────┼────────────┼─────┤
│  ◉   │ ✓ Upload│ ✓ Upload │ 125.5m  │280kh │   92.3%    │[Del]│
└──────┴─────────┴──────────┴─────────┴──────┴────────────┴─────┘
                                              ↑ НОВЫЙ СТОЛБЕЦ
```

### 3. Проверьте цвета

- **92.3%** должен быть **зеленым** и **жирным**
- **75.0%** должен быть **оранжевым**
- **55.0%** должен быть **красным** и **жирным**

---

## 🔍 Как это работает

### Backend (Python)

1. **parsing.py** - парсит файлы
2. **geometry.py** - вычисляет deviation для каждой точки
3. **processing.py** - вызывает C++ для каждой точки:
   ```python
   # Для каждой точки траектории
   result = subprocess.run([
       'cpp/trajectory_validator',
       lat, lon, alt, speed,
       seg_start_lat, seg_start_lon, seg_start_alt,
       seg_end_lat, seg_end_lon, seg_end_alt,
       allowed_deviation, allowed_speed
   ])
   
   # Парсит вывод: "deviation speed_violation is_compliant"
   deviation, speed_violation, is_compliant = result.stdout.split()
   ```

4. **processing.py** - подсчитывает процент:
   ```python
   compliant_count = sum(p['cpp_compliant'] for p in trajectory_points)
   compliance_percentage = (compliant_count / total_points) * 100
   ```

5. **models.py** - сохраняет в базу данных

### Frontend (JavaScript)

Отображает значение с цветовым кодированием:

```javascript
const percentage = flightCase.compliance_percentage.toFixed(1);
complianceCell.textContent = `${percentage}%`;

if (percentage >= 90) {
    complianceCell.style.color = '#4CAF50'; // Green
} else if (percentage >= 70) {
    complianceCell.style.color = '#FF9800'; // Orange
} else {
    complianceCell.style.color = '#F44336'; // Red
}
```

---

## 🐛 Устранение неполадок

### Проблема: Столбец не появляется

**Решение:**
1. Нажмите **Ctrl+F5** (принудительное обновление)
2. Очистите кэш браузера
3. Проверьте Console (F12) на наличие ошибок

### Проблема: Compliance показывает "Processing..."

**Причины:**
- C++ программа не скомпилирована
- C++ программа выдает ошибки

**Решение:**
```bash
cd cpp
make clean
make
./trajectory_validator 50.0 10.0 1000.0 250.0 50.1 10.1 1000.0 50.2 10.2 1000.0 500.0 300.0

# Должен вывести: deviation speed_violation is_compliant
# Например: 15724.32 0.00 1
```

### Проблема: Compliance = 0%

**Причины:**
- C++ не вернул результаты
- Траектория сильно отклоняется от коридора
- Скорость превышена на всех точках

**Проверка:**
1. Откройте admin: http://127.0.0.1:8000/admin/
2. Найдите FlightCase
3. Проверьте `trajectory_data` - должно быть поле `cpp_compliant` для каждой точки

### Проблема: Migration errors

**Ошибка:** "Field 'compliance_percentage' doesn't have a default value"

**Решение:**
```bash
python manage.py makemigrations
python manage.py migrate

# Если не помогло - удалите старые данные
rm db.sqlite3
python manage.py migrate
```

---

## 📊 Примеры результатов

### Хороший полет (Compliant)

```
Corridor: sample_data/corridor.txt
Trajectory: sample_data/trajectory.txt

Results:
├─ Mean Deviation: 125.5 m
├─ Mean Speed: 280.0 km/h
└─ Compliance: 92.3% 🟢 (отлично!)
```

### Полет с нарушениями

```
Corridor: sample_data/corridor_violation.txt
Trajectory: sample_data/trajectory_violation.txt

Results:
├─ Mean Deviation: 350.8 m
├─ Mean Speed: 320.0 km/h
└─ Compliance: 45.2% 🔴 (требуется проверка!)
```

---

## 🎯 Интерпретация результатов

| Compliance | Оценка | Действия |
|-----------|--------|----------|
| 95-100% | ⭐⭐⭐ Отлично | Идеальный полет |
| 90-95% | ⭐⭐ Хорошо | Минимальные отклонения |
| 70-90% | ⭐ Приемлемо | Проверить причины отклонений |
| 50-70% | ⚠️ Внимание | Значительные нарушения |
| < 50% | 🚨 Критично | Требуется разбор полетов |

---

## 📚 Технические детали

### Структура таблицы

| Столбец | Ширина | Описание |
|---------|--------|----------|
| Select | 5% | Радио-кнопка выбора |
| Corridor File | 19% | Файл коридора |
| Trajectory File | 19% | Файл траектории |
| Mean Deviation | 13% | Среднее отклонение (м) |
| Mean Speed | 13% | Средняя скорость (км/ч) |
| **Compliance** | **13%** | **Процент соответствия (%)** |
| Actions | 10% | Кнопка удаления |

### API Response

Теперь `/api/flight-cases/` возвращает дополнительное поле:

```json
{
  "id": 1,
  "mean_deviation": 125.5,
  "mean_speed": 280.0,
  "max_speed": 310.0,
  "compliance_percentage": 92.3,  ← НОВОЕ ПОЛЕ
  "is_processed": true,
  ...
}
```

### Database Schema

Добавлено новое поле в таблицу `monitoring_flightcase`:

```sql
compliance_percentage REAL NULL
```

---

## ✅ Контрольный список обновления

После применения обновления проверьте:

- [ ] C++ программа перекомпилирована (`make clean && make`)
- [ ] Миграция выполнена (`makemigrations` + `migrate`)
- [ ] Сервер перезапущен
- [ ] Браузер обновлен (Ctrl+F5)
- [ ] В таблице 7 столбцов (раньше было 6)
- [ ] Новый столбец называется "Compliance"
- [ ] При загрузке файлов появляется процент
- [ ] Процент окрашен в соответствующий цвет
- [ ] Tooltip при наведении на заголовок показывает подсказку

---

## 🔄 Откат изменений (если нужно)

Если что-то пошло не так и нужно вернуться:

```bash
# Восстановить старую версию C++
cd cpp
git checkout HEAD~1 trajectory_validator.cpp
make

# Откатить миграцию
python manage.py migrate monitoring <предыдущая_миграция>

# Восстановить старую версию файлов
git checkout HEAD~1 monitoring/models.py
git checkout HEAD~1 monitoring/processing.py
git checkout HEAD~1 templates/index.html
```

---

**FOFIS v1.2.0** - Compliance Percentage Feature  
*Рассчитывается C++ для максимальной производительности*

🎉 Наслаждайтесь новой функцией! 🚁✈️

