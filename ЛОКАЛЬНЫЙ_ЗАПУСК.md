# 🚀 Локальный запуск FOFIS

## Вариант 1: Автоматический запуск (1 команда)

```bash
cd /Users/ivan/PycharmProjects/FOFIS
chmod +x setup.sh
./setup.sh
```

Скрипт автоматически:
- ✅ Создаст виртуальное окружение
- ✅ Установит все зависимости
- ✅ Скомпилирует C++ валидатор
- ✅ Создаст базу данных
- ✅ Выполнит миграции

После этого запустите сервер:
```bash
source venv/bin/activate
python manage.py runserver
```

---

## Вариант 2: Ручной запуск (пошагово)

### Шаг 1: Перейдите в директорию проекта
```bash
cd /Users/ivan/PycharmProjects/FOFIS
```

### Шаг 2: Активируйте виртуальное окружение
```bash
source venv/bin/activate
```

Если виртуальное окружение не создано, создайте его:
```bash
python3 -m venv venv
source venv/bin/activate
```

### Шаг 3: Установите зависимости (если еще не установлены)
```bash
pip install -r requirements.txt
```

### Шаг 4: Скомпилируйте C++ валидатор (если не скомпилирован)
```bash
cd cpp
make
cd ..
```

### Шаг 5: Выполните миграции (если база данных не создана)
```bash
python manage.py migrate
```

### Шаг 6: Запустите сервер
```bash
python manage.py runserver
```

### Шаг 7: Откройте в браузере
```
http://127.0.0.1:8000/
```

---

## 🎯 Быстрая проверка работы

### 1. Загрузите тестовые файлы
В браузере на главной странице:
- **Corridor**: выберите `sample_data/corridor.txt`
- **Trajectory**: выберите `sample_data/trajectory.txt`
- Нажмите кнопку загрузки

### 2. Проверьте результаты
- Траектория появится на карте (синяя линия)
- Коридор появится на карте (зеленая линия)
- В таблице увидите метрики (deviation, speed)

### 3. Запустите анимацию
- Нажмите на строку в таблице
- Нажмите кнопку **▶ Play**
- Самолет будет двигаться по траектории

---

## 📋 Полезные команды

### Создать суперпользователя (для доступа в admin)
```bash
python manage.py createsuperuser
```
Затем откройте: http://127.0.0.1:8000/admin/

### Собрать статические файлы
```bash
python manage.py collectstatic
```

### Запустить тесты
```bash
pytest
```

### Проверить структуру БД
```bash
python manage.py dbshell
```

### Очистить базу данных
```bash
rm db.sqlite3
python manage.py migrate
```

### Посмотреть все роуты
```bash
python manage.py show_urls  # если установлен django-extensions
# или
python manage.py shell
>>> from django.urls import get_resolver
>>> print(get_resolver().url_patterns)
```

---

## 🧪 API Endpoints для тестирования

### Получить список случаев
```bash
curl http://127.0.0.1:8000/api/cases/
```

### Загрузить файлы через API
```bash
curl -X POST http://127.0.0.1:8000/api/test-upload/ \
  -F "corridor_file=@sample_data/corridor.txt" \
  -F "trajectory_file=@sample_data/trajectory.txt"
```

### Получить конкретный случай
```bash
curl http://127.0.0.1:8000/api/cases/1/
```

---

## 🔧 Troubleshooting

### Ошибка: "Address already in use"
Порт 8000 занят. Используйте другой:
```bash
python manage.py runserver 8001
```

### Ошибка: "No module named 'django'"
Активируйте виртуальное окружение:
```bash
source venv/bin/activate
pip install -r requirements.txt
```

### Ошибка: "cpp/trajectory_validator not found"
Скомпилируйте C++ код:
```bash
cd cpp
make clean
make
chmod +x trajectory_validator
cd ..
```

### Ошибка: "table monitoring_flightcase doesn't exist"
Выполните миграции:
```bash
python manage.py makemigrations
python manage.py migrate
```

### C++ валидатор не работает
Проверьте компиляцию:
```bash
cd cpp
./trajectory_validator 50.0 10.0 1000.0 250.0 50.1 10.1 1000.0 50.2 10.2 1000.0 500.0 300.0
# Должен вывести два числа: deviation и speed_violation
```

### Статические файлы не загружаются
```bash
python manage.py collectstatic --noinput
```

---

## 📂 Структура проекта

```
FOFIS/
├── fofis_project/        # Django проект
│   ├── settings.py       # Настройки
│   ├── urls.py          # URL routing
│   └── wsgi.py          # WSGI entry point
├── monitoring/           # Django приложение
│   ├── models.py        # Модели БД
│   ├── views.py         # Views
│   ├── serializers.py   # DRF serializers
│   └── processing.py    # Бизнес-логика
├── cpp/                 # C++ валидатор
│   ├── trajectory_validator.cpp
│   └── Makefile
├── templates/           # HTML шаблоны
├── static/             # Статические файлы
├── media/              # Загруженные файлы
├── sample_data/        # Тестовые данные
├── manage.py           # Django management
└── requirements.txt    # Python зависимости
```

---

## 🎯 Режимы работы

### Development (локально)
```bash
DEBUG=True
DATABASE=SQLite
ALLOWED_HOSTS=localhost,127.0.0.1
```

### Production (Render.com)
```bash
DEBUG=False
DATABASE=PostgreSQL
ALLOWED_HOSTS=your-app.onrender.com
```

---

## 📊 Порты и URL

| Сервис | URL | Описание |
|--------|-----|----------|
| **Web Server** | http://127.0.0.1:8000 | Главная страница |
| **API** | http://127.0.0.1:8000/api/ | REST API |
| **Admin** | http://127.0.0.1:8000/admin/ | Django Admin |
| **Static** | http://127.0.0.1:8000/static/ | CSS/JS файлы |
| **Media** | http://127.0.0.1:8000/media/ | Загруженные файлы |

---

## 🔐 Переменные окружения (опционально)

Создайте файл `.env`:
```bash
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
DATABASE_URL=sqlite:///db.sqlite3
```

Они будут автоматически загружены через `python-decouple`.

---

## 🎉 Готово!

После запуска сервера откройте:
```
http://127.0.0.1:8000/
```

Загрузите тестовые файлы из `sample_data/` и наслаждайтесь работой системы! ✈️

