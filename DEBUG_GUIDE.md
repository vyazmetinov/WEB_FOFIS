# 🐛 Руководство по отладке FOFIS

## Проблема: "Данные для демонстрации не выбраны"

### ✅ Что было исправлено

1. **Асинхронная загрузка данных** - теперь радио-кнопка правильно ждет завершения загрузки
2. **Валидация данных** - добавлены проверки на наличие и корректность данных
3. **Детальное логирование** - все действия выводятся в консоль браузера
4. **Совместимость полей** - данные сохраняются с обоими именами (`trajectory` и `trajectory_data`)

### 📋 Пошаговая проверка

#### Шаг 1: Откройте консоль браузера

**Нажмите F12** и перейдите на вкладку **Console**

#### Шаг 2: Перезагрузите страницу

Обновите страницу в браузере (F5 или Ctrl+R)

#### Шаг 3: Загрузите файлы

1. Перетащите `sample_data/corridor.txt` в dropzone
2. Перетащите `sample_data/trajectory.txt` в dropzone

**В консоли должно появиться:**
```
Loading flight case data for ID: 1
Flight case data loaded: {corridor: Array(8), trajectory: Array(21), ...}
Flight case loaded successfully. Play button enabled.
```

#### Шаг 4: Выберите траекторию

Кликните на радио-кнопку (◯) в колонке "Select"

**В консоли должно появиться:**
```
Loading flight case data for ID: 1
Flight case data loaded: {...}
Flight case loaded successfully. Play button enabled.
Flight case successfully loaded, ready for playback
```

#### Шаг 5: Нажмите Play

Кликните на кнопку **▶ Play**

**В консоли должно появиться:**
```
startPlayback called
currentFlightCase: {corridor: Array(8), trajectory: Array(21), trajectory_data: Array(21), ...}
Starting playback with 21 trajectory points
```

---

## 🔍 Диагностика проблем

### Проблема: "HTTP error! status: 400/404/500"

**Причина:** Сервер не отвечает или данные не обработаны

**Решение:**
```bash
# Проверьте, что сервер запущен
python manage.py runserver

# Проверьте логи Django в терминале
# Там будут ошибки обработки
```

### Проблема: "Invalid data: missing corridor or trajectory"

**Причина:** Файлы не были обработаны или обработаны с ошибкой

**Решение:**
1. Откройте admin: http://127.0.0.1:8000/admin/
2. Найдите ваш FlightCase
3. Проверьте поле `processing_error` - там будет описание ошибки
4. Проверьте, что `is_processed = True`

### Проблема: "No trajectory_data in currentFlightCase"

**Причина:** Данные не загрузились или формат ответа неправильный

**Решение:**
1. Откройте в новой вкладке: http://127.0.0.1:8000/api/flight-cases/1/trajectory_data/
2. Проверьте, что в ответе есть поля `corridor` и `trajectory`
3. Если нет - проверьте backend в `monitoring/views.py`

### Проблема: Радио-кнопка не выбирается

**Причина:** JavaScript ошибка или конфликт событий

**Решение:**
1. Проверьте Console на красные ошибки
2. Попробуйте выбрать другую строку
3. Обновите страницу (F5)

---

## 🧪 Ручное тестирование в консоли

Откройте Console (F12) и выполните:

### Проверка глобальных переменных

```javascript
// Проверить текущий flight case
console.log('currentFlightCase:', currentFlightCase);

// Проверить все загруженные cases
console.log('flightCases:', flightCases);

// Проверить времена
console.log('startTime:', startTime);
console.log('endTime:', endTime);
console.log('currentTime:', currentTime);
```

### Ручная загрузка данных

```javascript
// Загрузить данные вручную (замените 1 на ваш ID)
loadFlightCaseData(1).then(success => {
    console.log('Load successful:', success);
    console.log('currentFlightCase:', currentFlightCase);
});
```

### Проверка API напрямую

```javascript
// Получить данные напрямую с API
fetch('/api/flight-cases/1/trajectory_data/')
    .then(r => r.json())
    .then(data => console.log('API response:', data));
```

---

## 📊 Логи при нормальной работе

### При загрузке файлов:
```
Loading flight case data for ID: 1
Flight case data loaded: {
  corridor: Array(8),
  trajectory: Array(21),
  start_time: "13:00:00",
  end_time: "14:00:00",
  mean_speed: 285.5,
  mean_deviation: 1234.56
}
Flight case loaded successfully. Play button enabled.
Flight case successfully loaded, ready for playback
```

### При нажатии Play:
```
startPlayback called
currentFlightCase: {
  corridor: Array(8),
  trajectory: Array(21),
  trajectory_data: Array(21),
  corridor_data: Array(8),
  ...
}
Starting playback with 21 trajectory points
```

### Во время воспроизведения:
```
(Каждый кадр - лог не выводится для производительности)
```

---

## 🔧 Если ничего не помогает

### Полный сброс

```bash
# 1. Остановите сервер (Ctrl+C)

# 2. Удалите базу данных
rm db.sqlite3

# 3. Удалите загруженные файлы
rm -rf media/corridors/* media/trajectories/*

# 4. Пересоздайте базу
python manage.py migrate

# 5. Запустите сервер
python manage.py runserver

# 6. Откройте браузер и загрузите файлы заново
```

### Проверка backend

```bash
# Запустите тесты
python manage.py test

# Проверьте парсинг вручную
python manage.py shell

>>> from monitoring.parsers import parse_corridor_file, parse_trajectory_file
>>> corridor = parse_corridor_file('sample_data/corridor.txt')
>>> print(len(corridor))
8
>>> trajectory = parse_trajectory_file('sample_data/trajectory.txt')
>>> print(len(trajectory))
21
```

---

## 📞 Контрольный список

Перед обращением за помощью проверьте:

- [ ] Сервер запущен (`python manage.py runserver`)
- [ ] Консоль браузера открыта (F12)
- [ ] Нет красных ошибок в Console
- [ ] Файлы загружены (оба: corridor + trajectory)
- [ ] Строка обработана (нет "Processing...")
- [ ] Радио-кнопка выбрана (зеленая подсветка)
- [ ] В Console есть логи "Flight case loaded successfully"
- [ ] Кнопка Play не disabled

---

## 🎯 Быстрая проверка работоспособности

1. **F12** - открыть консоль
2. **F5** - обновить страницу
3. Загрузить оба файла
4. Дождаться зеленой подсветки
5. Посмотреть Console - должен быть лог "Flight case successfully loaded"
6. Нажать Play
7. Посмотреть Console - должен быть лог "Starting playback with X trajectory points"

Если все логи есть - система работает правильно! 🎉

---

**Примечание:** Все изменения включают расширенное логирование. Всегда держите консоль открытой для отладки.

