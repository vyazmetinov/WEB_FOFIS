# 🎯 LIVE COMPLIANCE - МОНИТОРИНГ В РЕАЛЬНОМ ВРЕМЕНИ

## Что это?

**Live Compliance** - это индикатор в реальном времени, который показывает процент соответствия траектории коридору **по мере движения самолета** во время воспроизведения.

---

## 🎨 Где находится

Live Compliance отображается в **Playback Controls** (правый верхний угол) под элементами управления воспроизведением:

```
╔═══════════════════════════════════════╗
║      PLAYBACK CONTROLS                ║
╠═══════════════════════════════════════╣
║  [▶ Play] [⏸ Pause] [Speed: 1×]     ║
║  [━━━━━━━━━━━━━━━━━━] Time Slider    ║
║  13:15:30 / 14:00:00                  ║
║                                       ║
║  ┌─────────────────────────────────┐ ║
║  │ LIVE COMPLIANCE                 │ ║
║  │ 87.5%  ←────────────────────────┤ ║
║  │ [████████████░░░░] Progress Bar │ ║
║  └─────────────────────────────────┘ ║
╚═══════════════════════════════════════╝
```

---

## 📊 Как это работает

### 1. Расчет в реальном времени

При каждом кадре анимации (60 FPS):

1. **Определяется текущая точка** траектории
2. **Подсчитывается compliance** только для **пройденных точек**
3. **Обновляется индикатор** с новым значением

### 2. Формула

```
Compliance = (Количество compliant точек / Всего пройденных точек) × 100%
```

**Точка считается compliant если:**
- Deviation ≤ Allowed Deviation (C++) ✅
- Speed ≤ Allowed Speed (C++) ✅

### 3. Динамика

```
Начало:     0.0%  (0 из 0 точек)
           ↓
Точка 1:   100%   (1 из 1 - compliant)
           ↓
Точка 2:   100%   (2 из 2 - обе compliant)
           ↓
Точка 3:   66.7%  (2 из 3 - одна не compliant)
           ↓
Конец:     87.5%  (18 из 21 compliant)
```

---

## 🎨 Визуализация

### Цветовое кодирование

| Значение | Цвет | Значение | Граница |
|----------|------|----------|---------|
| ≥ 90% | 🟢 Зеленый | Отлично | Зеленая |
| 70-90% | 🟠 Оранжевый | Хорошо | Оранжевая |
| < 70% | 🔴 Красный | Внимание | Красная |

### Элементы интерфейса

1. **Заголовок**: "LIVE COMPLIANCE"
2. **Значение**: Большие цифры (например, "87.5%")
3. **Progress Bar**: Заполняется слева направо
4. **Цветная граница**: Меняется в зависимости от значения

---

## 🎮 Поведение

### При старте воспроизведения
- Compliance = **0.0%**
- Цвет: синий (нейтральный)
- Полоса пуста

### Во время воспроизведения
- Обновляется каждый кадр
- Растет от 0% к финальному значению
- Цвет меняется динамически

### При паузе
- Сохраняется текущее значение
- Не обновляется

### При перемотке (Time Slider)
- Пересчитывается для новой позиции
- Показывает compliance до этого момента

### При остановке/сбросе
- Возвращается к **0.0%**
- Цвет: синий
- Полоса обнуляется

---

## 💡 Практическое применение

### Сценарий 1: Идеальный полет

```
Начало: 0.0%    [░░░░░░░░░░]
10%:    100%    [██████████]  🟢 Отлично!
50%:    98.5%   [██████████]  🟢 Все хорошо
100%:   96.2%   [██████████]  🟢 Успешный полет
```

### Сценарий 2: Нарушение в середине

```
Начало: 0.0%    [░░░░░░░░░░]
20%:    100%    [██████████]  🟢 Начало хорошее
40%:    95.0%   [█████████░]  🟢 Еще норма
50%:    68.0%   [██████░░░░]  🔴 Нарушение!
100%:   75.0%   [███████░░░]  🟠 Требуется проверка
```

### Сценарий 3: Критические отклонения

```
Начало: 0.0%    [░░░░░░░░░░]
10%:    50.0%   [█████░░░░░]  🔴 Сразу проблемы
50%:    45.0%   [████░░░░░░]  🔴 Ухудшается
100%:   42.0%   [████░░░░░░]  🔴 Критично!
```

---

## 🔍 Технические детали

### Функция updateLiveCompliance()

```javascript
function updateLiveCompliance(trajectory, currentIndex) {
    let compliantCount = 0;
    let totalCount = currentIndex + 1;
    
    // Подсчет compliant точек
    for (let i = 0; i <= currentIndex; i++) {
        const point = trajectory[i];
        const isCompliant = point.cpp_compliant || 
            (point.deviation <= point.allowed_deviation &&
             point.speed <= point.allowed_speed);
        
        if (isCompliant) {
            compliantCount++;
        }
    }
    
    // Расчет процента
    const percentage = (compliantCount / totalCount) * 100;
    
    // Обновление UI
    valueElement.textContent = `${percentage.toFixed(1)}%`;
    fillElement.style.width = `${percentage}%`;
    
    // Цветовое кодирование
    if (percentage >= 90) {
        // Зеленый
    } else if (percentage >= 70) {
        // Оранжевый
    } else {
        // Красный
    }
}
```

### Вызывается из:

1. **updatePlayback()** - каждый кадр анимации
2. **setPlaybackTime()** - при перемотке
3. **stopPlayback()** - сброс через resetLiveCompliance()

---

## 🎯 Преимущества Live Compliance

### 1. Мониторинг в реальном времени
- Видно, когда именно начались нарушения
- Можно остановить и проанализировать момент

### 2. Динамическая визуализация
- Интуитивно понятно
- Цвета привлекают внимание к проблемам

### 3. Накопительная метрика
- Показывает общую картину
- Не зависит от одной точки

### 4. C++ расчеты
- Быстро и точно
- Учитывает и deviation, и speed

---

## 📱 Примеры использования

### Оператор мониторинга

1. Загружает траекторию
2. Нажимает Play
3. Наблюдает за Live Compliance
4. При падении < 70%:
   - Останавливает
   - Проверяет карту
   - Анализирует причину

### Анализ записи

1. Перематывает к интересному участку
2. Видит compliance до этого момента
3. Запускает воспроизведение
4. Следит за изменениями

### Сравнение полетов

1. Загружает первую траекторию → compliance 92%
2. Загружает вторую траекторию → compliance 68%
3. Видит разницу в реальном времени

---

## ⚙️ Настройки

### Частота обновления
**60 FPS** - обновляется при каждом кадре анимации

### Точность отображения
**0.1%** - округление до одного знака после запятой

### Пороги цветов
```javascript
>= 90%  → Зеленый  (#4CAF50)
70-90%  → Оранжевый (#FF9800)
< 70%   → Красный  (#F44336)
```

---

## 🐛 Отладка

### Проблема: Compliance не обновляется

**Причина:** Данные точек не содержат информацию о compliance

**Решение:**
```javascript
// В Console (F12)
console.log(currentFlightCase.trajectory_data[0]);

// Должно быть:
{
  deviation: 125.5,
  allowed_deviation: 500,
  speed: 280,
  allowed_speed: 300,
  cpp_compliant: true  ← Это поле от C++
}
```

### Проблема: Compliance всегда 0%

**Причина:** C++ программа не работает или не возвращает is_compliant

**Решение:**
```bash
# Проверьте C++
cd cpp
./trajectory_validator 50.0 10.0 1000.0 250.0 \
                       50.1 10.1 1000.0 50.2 10.2 1000.0 \
                       500.0 300.0

# Должен вывести 3 числа, третье - 0 или 1
# Например: 13214.51 0.00 1
```

### Проблема: Compliance не меняет цвет

**Проверьте в Console:**
```javascript
document.getElementById('live-compliance-value').style.color;
// Должен быть: rgb(76, 175, 80) для зеленого
```

---

## 🎉 Итого

**Live Compliance** превращает простой playback в инструмент **реального мониторинга**:

✅ Видно соответствие **в процессе полета**  
✅ Можно **остановить в нужный момент**  
✅ **Цвета** указывают на проблемы  
✅ **Progress bar** показывает визуально  
✅ **C++ точность** расчетов  

---

**Попробуйте:**

1. Загрузите `sample_data/corridor.txt` + `trajectory.txt`
2. Нажмите **▶ Play**
3. Следите за **Live Compliance** в правом верхнем углу
4. Наблюдайте, как он растет от 0% к ~85-90%

🎯 Теперь вы видите compliance **в реальном времени**!




