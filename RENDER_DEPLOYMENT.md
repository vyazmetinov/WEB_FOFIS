# 🚀 Деплой FOFIS на Render.com

Полная инструкция по развертыванию проекта FOFIS (Flight Observation and Flight path Inspection System) на платформе Render.com.

## ✨ Преимущества Render.com для этого проекта

- ✅ **Поддержка C++ executables** - траекторный валидатор будет работать
- ✅ **Бесплатный PostgreSQL** - настоящая база данных вместо SQLite
- ✅ **Persistent disk** - 1GB для хранения загруженных файлов
- ✅ **750 часов бесплатно** в месяц (достаточно для одного сервиса)
- ✅ **Автоматический деплой** из GitHub
- ✅ **SSL сертификаты** бесплатно

## 📋 Подготовка (уже сделано)

Все необходимые файлы уже созданы:

- ✅ `render.yaml` - конфигурация для Render
- ✅ `build_render.sh` - скрипт сборки (компиляция C++, миграции, статика)
- ✅ `requirements.txt` - обновлен с PostgreSQL зависимостями
- ✅ `settings.py` - настроен для работы с PostgreSQL и WhiteNoise
- ✅ `cpp/Makefile` - для компиляции C++ валидатора
- ✅ `.env.example` - пример переменных окружения

## 🎯 Шаги для деплоя

### 1. Подготовка репозитория

Если еще не сделано, закоммитьте все изменения и отправьте на GitHub:

```bash
# Добавьте .env в .gitignore (если еще не добавлен)
echo ".env" >> .gitignore
echo "*.env" >> .gitignore
echo "db.sqlite3" >> .gitignore

# Закоммитьте все изменения
git add .
git commit -m "Configure for Render.com deployment"
git push origin main
```

### 2. Регистрация на Render.com

1. Перейдите на [render.com](https://render.com)
2. Нажмите **"Get Started"** или **"Sign Up"**
3. Войдите через GitHub (рекомендуется)
4. Предоставьте Render доступ к вашим репозиториям

### 3. Создание нового проекта

Render автоматически обнаружит `render.yaml` и создаст все необходимые сервисы.

#### Вариант A: Blueprint (автоматический, рекомендуется)

1. На Dashboard нажмите **"New +"** → **"Blueprint"**
2. Подключите ваш GitHub репозиторий FOFIS
3. Render найдет `render.yaml` и покажет, что будет создано:
   - Web Service: `fofis`
   - PostgreSQL Database: `fofis-db`
4. Нажмите **"Apply"**
5. Render автоматически:
   - Создаст базу данных
   - Скомпилирует C++ код
   - Установит зависимости
   - Запустит миграции
   - Задеплоит приложение

#### Вариант B: Ручное создание

Если Blueprint не работает:

1. **Создайте PostgreSQL базу:**
   - New + → PostgreSQL
   - Name: `fofis-db`
   - Database: `fofis`
   - User: `fofis`
   - Plan: Free
   - Create Database

2. **Создайте Web Service:**
   - New + → Web Service
   - Connect Repository: выберите FOFIS
   - Name: `fofis`
   - Environment: Python
   - Region: Frankfurt (или ближайший к вам)
   - Branch: `main`
   - Build Command: `bash build_render.sh`
   - Start Command: `gunicorn fofis_project.wsgi:application --bind 0.0.0.0:$PORT`
   - Plan: Free
   - Add Disk:
     - Name: `fofis-media`
     - Mount Path: `/opt/render/project/src/media`
     - Size: 1GB

3. **Настройте Environment Variables:**
   - `PYTHON_VERSION` = `3.12.0`
   - `SECRET_KEY` = (generate new) - используйте кнопку Generate
   - `DEBUG` = `False`
   - `DATABASE_URL` = (подключите к созданной БД)
   - `DJANGO_SETTINGS_MODULE` = `fofis_project.settings`

4. **Create Web Service**

### 4. Первый деплой

После создания сервиса:

1. Render начнет автоматический деплой
2. Следите за логами (Logs tab)
3. Процесс займет 3-5 минут:
   ```
   📦 Installing Python dependencies...
   🔧 Building C++ trajectory validator...
   📊 Collecting static files...
   🗄️ Running database migrations...
   ✅ Build completed successfully!
   ```

4. После успешного деплоя получите URL вида: `https://fofis.onrender.com`

### 5. Проверка работы

Откройте ваш URL и проверьте:

- ✅ Главная страница загружается
- ✅ Можно загрузить trajectory и corridor файлы
- ✅ Статические файлы (CSS/JS) работают
- ✅ API endpoints отвечают

Тестовые endpoints:
```
https://your-app.onrender.com/
https://your-app.onrender.com/api/cases/
https://your-app.onrender.com/api/test-upload/
```

### 6. Создание суперпользователя (опционально)

Если нужен доступ к Django Admin:

1. В Render Dashboard откройте ваш Web Service
2. Перейдите в **Shell** tab
3. Выполните команду:
   ```bash
   python manage.py createsuperuser
   ```
4. Заполните данные (username, email, password)
5. Теперь можете войти на `https://your-app.onrender.com/admin/`

## ⚙️ Дополнительные настройки

### Автоматический деплой из GitHub

По умолчанию включен автоматический деплой при push в main:

- Каждый `git push` в ветку `main` автоматически запустит деплой
- Можно отключить в Settings → Auto-Deploy

### Добавление custom domain

1. В Settings → Custom Domain
2. Добавьте ваш домен
3. Настройте DNS записи (CNAME или A record)
4. SSL сертификат выдается автоматически

### Мониторинг и логи

- **Logs**: Вкладка Logs показывает логи в реальном времени
- **Metrics**: Использование CPU, памяти, bandwidth
- **Events**: История деплоев и событий

### Disk (persistent storage)

Ваши загруженные файлы хранятся на persistent disk:
- Путь: `/opt/render/project/src/media`
- Размер: 1GB (free tier)
- Файлы сохраняются между деплоями

## 🔧 Обслуживание

### Обновление приложения

```bash
# Внесите изменения в код
git add .
git commit -m "Your changes"
git push origin main

# Render автоматически задеплоит обновления
```

### Запуск миграций вручную

Если нужно запустить миграции отдельно:

1. Shell → 
   ```bash
   python manage.py migrate
   ```

### Откат к предыдущей версии

1. Events → найдите предыдущий успешный деплой
2. Нажмите **"Rollback to this version"**

### Просмотр логов

```bash
# В Shell tab
tail -f /var/log/render.log
```

## ⚠️ Важные ограничения Free Tier

### 1. "Засыпание" сервиса

- После **15 минут неактивности** сервис переходит в спящий режим
- Первый запрос после сна будет долгим (~30 секунд)
- Решения:
  - Upgrade на платный план ($7/месяц)
  - Использовать UptimeRobot для периодических пингов (но это против ToS)

### 2. База данных

- **90 дней** хранения для free PostgreSQL
- После этого нужно создать новую (можно экспортировать/импортировать данные)
- Или upgrade на платный план

### 3. Ресурсы

- 512 MB RAM
- 0.1 CPU
- 1 GB Disk
- Достаточно для тестирования и небольших проектов

## 🐛 Troubleshooting

### Проблема: Build fails при компиляции C++

**Решение:**
```bash
# В Shell проверьте наличие g++
which g++
g++ --version

# Если нет, в render.yaml добавьте в buildCommand:
apt-get update && apt-get install -y build-essential && bash build_render.sh
```

### Проблема: Static files не загружаются

**Решение:**
1. Проверьте, что WhiteNoise в MIDDLEWARE
2. В Shell выполните:
   ```bash
   python manage.py collectstatic --noinput
   ```

### Проблема: Database connection error

**Решение:**
1. Проверьте Environment Variables → DATABASE_URL
2. Должен быть в формате: `postgresql://user:password@host/database`
3. Убедитесь, что psycopg2-binary установлен

### Проблема: Media files не сохраняются

**Решение:**
1. Проверьте, что Disk подключен в настройках сервиса
2. Mount Path должен быть: `/opt/render/project/src/media`
3. В settings.py проверьте MEDIA_ROOT

### Проблема: 500 Internal Server Error

**Решение:**
1. Проверьте логи: Logs tab
2. Убедитесь что DEBUG=False и SECRET_KEY установлен
3. Проверьте ALLOWED_HOSTS

## 📊 Мониторинг производительности

### Полезные команды в Shell

```bash
# Проверка статуса
python manage.py check

# Список миграций
python manage.py showmigrations

# Тест БД соединения
python manage.py dbshell

# Статистика БД
python manage.py sqlflush

# Размер media файлов
du -sh /opt/render/project/src/media

# Проверка C++ валидатора
./cpp/trajectory_validator 50.0 10.0 1000.0 250.0 50.1 10.1 1000.0 50.2 10.2 1000.0 500.0 300.0
```

## 🔐 Безопасность

### Обязательно перед продакшеном:

- ✅ Установите настоящий SECRET_KEY (не default)
- ✅ DEBUG = False
- ✅ Настройте правильные ALLOWED_HOSTS
- ✅ Используйте environment variables для секретов
- ✅ Регулярно обновляйте зависимости
- ✅ Настройте backup базы данных

### Генерация SECRET_KEY

```python
# В Python shell
from django.core.management.utils import get_random_secret_key
print(get_random_secret_key())
```

## 🚀 Upgrade на платный план

Если нужно больше ресурсов:

**Starter Plan ($7/месяц):**
- Не засыпает
- 512 MB RAM
- 0.5 CPU

**Standard Plan ($25/месяц):**
- 2 GB RAM
- 1 CPU
- Priority support

## 📚 Дополнительные ресурсы

- [Render Documentation](https://render.com/docs)
- [Deploy Django on Render](https://render.com/docs/deploy-django)
- [Render Community](https://community.render.com/)
- [Django Deployment Checklist](https://docs.djangoproject.com/en/4.2/howto/deployment/checklist/)

## 🎉 Готово!

Ваш проект FOFIS теперь задеплоен на Render.com и доступен онлайн!

**URL:** https://your-app.onrender.com

Поделитесь ссылкой и используйте систему мониторинга полетных коридоров! ✈️


